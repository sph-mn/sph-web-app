; the config file supports all literal scheme syntax as it is parsed with scheme read. but nothing is evaluated.
; if specified for loading it will be available as a hashtable in the swa-env object passed to app-respond.
; format is key and value alternatingly: key value key/value ...

development #t
